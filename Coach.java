/**
 * Write a description of class Coach here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Coach extends Human
{
    // There are no field variables for the coach as of yet. Add it when it is deemed as necessary! 

    /**
     * Constructor for objects of class Coach
     * The constructor assisgns a name to the Coach.
     */
    public Coach(String name)
    {
        // initialise instance variables
        super(name);
    }
}
